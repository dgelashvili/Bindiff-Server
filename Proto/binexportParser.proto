syntax = "proto3";

package binexportParser;

service Parser {
  rpc Parse (ParseRequest) returns (ParseReply);
}

message ParseRequest {
  bytes content = 1;
}

message ParseReply {
  repeated string names = 1;
}